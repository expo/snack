import { SDKVersion } from './types';

export const defaultSdkVersion: SDKVersion = '43.0.0';

// Mostly used for tests
export const oldestSdkVersion: SDKVersion = '41.0.0';
export const newestSdkVersion: SDKVersion = '43.0.0';
