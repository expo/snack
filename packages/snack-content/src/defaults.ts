import { SDKVersion } from './types';

export const defaultSdkVersion: SDKVersion = '48.0.0';

// Mostly used for tests
export const oldestSdkVersion: SDKVersion = '46.0.0';
export const newestSdkVersion: SDKVersion = '48.0.0';
