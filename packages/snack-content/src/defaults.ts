import { SDKVersion } from './types';

export const defaultSdkVersion: SDKVersion = '54.0.0'; // TODO: change this to SDK 55 when stable is out

// Mostly used for tests
export const oldestSdkVersion: SDKVersion = '50.0.0';
export const newestSdkVersion: SDKVersion = '55.0.0';
