const { System, applyImportMap, setBaseUrl } = require('systemjs');

/** @type {import('systemjs').System} */
const system = new System.constructor();

// Add App.js file
system.set('module://App.js', )
